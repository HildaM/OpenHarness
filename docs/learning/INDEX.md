# 源码学习路线总结

> 按编号顺序阅读，从外壳层 → 通信层 → 核心层逐步深入。
> 每篇文档都标注了涉及的源文件、预计阅读时间和核心收获。

---

## 阅读路线图

```
你在这里
   │
   ▼
┌──────────────────────────────────────────────────────────┐
│ 第一阶段：外壳层（了解「怎么启动的」）                       │
│                                                           │
│   01 全景概览 → 02 CLI 入口 → 03 UI 路由                  │
│                                                           │
│   收获：知道 oh 命令从执行到进入核心的完整路径              │
└───────────────────────┬──────────────────────────────────┘
                        │
┌───────────────────────▼──────────────────────────────────┐
│ 第二阶段：通信层（了解「前后端怎么配合的」）                │
│                                                           │
│   04 前端启动器 → 05 双进程通信协议                        │
│                                                           │
│   收获：知道 Python 后端和 Node.js 前端如何通过 JSON 通信  │
└───────────────────────┬──────────────────────────────────┘
                        │
┌───────────────────────▼──────────────────────────────────┐
│ 第三阶段：核心层（了解「Agent 怎么工作的」）                │
│                                                           │
│   06 运行时装配 + Agent 循环 → 07 engine 包全景           │
│   → 08 对话压缩与成本追踪                                  │
│                                                           │
│   收获：完全理解 build_runtime → handle_line → run_query  │
└───────────────────────┬──────────────────────────────────┘
                        │
┌───────────────────────▼──────────────────────────────────┐
│ 第四阶段：子系统层（了解「工具怎么工作的」）                │
│                                                           │
│   10 工具系统架构 → 11 工具实现原理                        │
│                                                           │
│   收获：三层架构 + 字符串替换为什么是最佳方案 + 自己写工具  │
└───────────────────────┬──────────────────────────────────┘
                        │
┌───────────────────────▼──────────────────────────────────┐
│ 第五阶段：子系统层（了解「LLM 怎么调用的」）              │
│                                                           │
│   12 API 客户端深度剖析                                   │
│                                                           │
│   收获：Protocol 策略模式 + 三种客户端适配 + 流式传输     │
│         + 重试机制 + 消息格式翻译 + OAuth 设备流          │
└───────────────────────┬──────────────────────────────────┘
                        │
┌───────────────────────▼──────────────────────────────────┐
│ 专题（解答学习过程中的深层疑问）                            │
│                                                           │
│   09 为什么用 yield                                        │
└──────────────────────────────────────────────────────────┘
```

---

## 文档清单

### 第一阶段：外壳层

| # | 文件名 | 主题 | 涉及源文件 | 时间 | 核心收获 |
|---|--------|------|-----------|------|---------|
| 01 | [01-startup-overview.md](01-startup-overview.md) | 启动流程全景（7 层调用栈） | `__main__.py` → `cli.py` → `app.py` → `runtime.py` → `query.py` | 30min | 整个启动链路的鸟瞰图，知道代码在哪一层 |
| 02 | [02-cli-entry-point.md](02-cli-entry-point.md) | `cli.py` main() 入口深度剖析 | `cli.py` 第 416-681 行 | 25min | 20 个 CLI 参数 + 三条执行路径（交互/非交互/恢复） |
| 03 | [03-app-ui-routing.md](03-app-ui-routing.md) | `ui/app.py` UI 路由层 | `ui/app.py` + `engine/stream_events.py` | 20min | run_repl vs run_print_mode + 4 种 StreamEvent + 实验日志分析 |

### 第二阶段：通信层

| # | 文件名 | 主题 | 涉及源文件 | 时间 | 核心收获 |
|---|--------|------|-----------|------|---------|
| 04 | [04-react-launcher.md](04-react-launcher.md) | 前端启动器 | `ui/react_launcher.py` + 前端 `index.tsx` | 15min | Python 如何 spawn Node.js 前端 + 环境变量传参 |
| 05 | [05-frontend-backend-protocol.md](05-frontend-backend-protocol.md) | 前后端通信协议（7 文件联合） | `backend_host.py` + `protocol.py` + `useBackendSession.ts` + `App.tsx` 等 | 40min | OHJSON 协议 + 14 种事件 + 权限弹窗往返 + 流式渲染 |

### 第三阶段：核心层

| # | 文件名 | 主题 | 涉及源文件 | 时间 | 核心收获 |
|---|--------|------|-----------|------|---------|
| 06 | [06-runtime-and-agent-loop.md](06-runtime-and-agent-loop.md) | runtime 装配 + Agent 循环 | `runtime.py` + `query_engine.py` + `query.py` | 35min | build_runtime 11 步 + handle_line 两条分支 + run_query 4 阶段 + 工具执行 6 道关卡 |
| 07 | [07-engine-package.md](07-engine-package.md) | engine/ 包 6 文件全景 | engine/ 全部 6 个文件 | 25min | 文件间依赖图 + 对外接口清单 + 消息历史生命周期 + 7 个设计模式 |
| 08 | [08-engine-compact-and-cost.md](08-engine-compact-and-cost.md) | 消息模型 + 对话压缩 + 成本追踪 | `messages.py` + `compact/` + `cost_tracker.py` + `token_estimation.py` | 30min | 3 种 ContentBlock + 两级压缩策略 + 阈值 167K + Token 估算 |

### 第四阶段：子系统层（工具系统）

| # | 文件名 | 主题 | 涉及源文件 | 时间 | 核心收获 |
|---|--------|------|-----------|------|---------|
| 10 | [10-tool-system.md](10-tool-system.md) | 工具系统深度剖析 | `tools/base.py` + `tools/__init__.py` + 6 个工具实现 + `engine/query.py` | 35min | 三层架构（抽象→实现→注册）+ Pydantic 一举三得 + MCP 适配器模式 + 自己写工具 |
| 11 | [11-tool-deep-dive.md](11-tool-deep-dive.md) | 工具实现原理深入分析 | `tools/file_read_tool.py` + `tools/file_edit_tool.py` + `tools/bash_tool.py` + `utils/shell.py` | 25min | ToolInput 分离设计 + 文件读取全链路 + 命令执行链路 + 字符串替换为什么是最佳方案 |

### 第五阶段：子系统层（API 客户端）

| # | 文件名 | 主题 | 涉及源文件 | 时间 | 核心收获 |
|---|--------|------|-----------|------|---------|
| 12 | [12-api-client.md](12-api-client.md) | API 客户端深度剖析 | `api/client.py` + `api/openai_client.py` + `api/copilot_client.py` + `api/copilot_auth.py` + `api/provider.py` + `api/errors.py` + `api/usage.py` | 40min | Protocol 策略模式 + 三种客户端适配 + Anthropic↔OpenAI 格式翻译 + 流式传输 + 重试机制 + OAuth 设备流 |

### 专题

| # | 文件名 | 主题 | 时间 | 核心收获 |
|---|--------|------|------|---------|
| 09 | [09-why-yield.md](09-why-yield.md) | 为什么整个项目都用 yield | 15min | 三种方案对比 + 4 层穿透 + 水管比喻 + async for 语法入门 |

---

## 复习建议

### 快速复习（15 分钟）

只看 **01** 的全景图 + **06** 的数据流全景图 + **07** 的对外接口表。

### 中等复习（45 分钟）

**01** → **05**（通信协议的时序图）→ **06**（handle_line + run_query）→ **08**（压缩系统）。

### 完整复习（3 小时）

按编号 **01-09** 顺序通读，跳过已熟悉的部分。

---

## 总阅读量统计

| 指标 | 数值 |
|------|------|
| 文档总数 | 12 篇 |
| 总字数 | ~240,000 字符 |
| 预计总阅读时间 | ~5 小时 |
| 涉及源文件 | ~26 个核心文件 |
| 覆盖代码行 | ~3,800 行（engine + ui + prompts + tools + api + utils） |

---

*最后更新：2026-04-07*

---

**其他**：[PROGRESS.md](PROGRESS.md) — 学习进度与后续 5 个方向记录
